name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/**'
  workflow_dispatch:

# NOTE: Edge functions are deployed from the SantoNinoNZ/admin repo.
# This workflow is kept for reference but does nothing to avoid broken
# supabase link calls (the SUPABASE_PROJECT_REF secret here is stale).
# See: .github/workflows/functions-deploy.yml in the admin repo.

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Edge functions managed by admin repo
        run: |
          echo "Edge functions are deployed from the SantoNinoNZ/admin repository."
          echo "See https://github.com/SantoNinoNZ/admin/blob/main/.github/workflows/functions-deploy.yml"
